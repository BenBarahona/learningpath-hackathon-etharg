// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract MultipleChoice {

    uint256 public firebaseId;
    uint256 public requiredTokens;
    uint256 public challengeReward;

    constructor(uint256 _firebaseId, uint256 _tokenAmountRequired, uint256 _challengeReward) {
        firebaseId = _firebaseId;
        requiredTokenAmount = _tokenAmountRequired;
        challengeReward = _challengeReward;
    }

    function awardTokensToUser(address user, uint256 amount) {
    {
        
    }
}